[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
preDeployCommand = "php artisan migrate --force --no-interaction"
startCommand = "/entrypoint.sh sh -c 'php artisan serve --host=0.0.0.0 --port=${PORT:-8000}'"
restartPolicyType = "ON_FAILURE"
